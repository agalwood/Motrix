# LAN side (see upnp-v1/miniupnpd.conf for why an interface name is
# required); ext must differ, and is fake anyway.
listening_ip=eth0
ext_ifname=lo
enable_natpmp=yes
enable_upnp=no
# Minimal HTTP port — required by miniupnpd but not used
http_port=49200
friendly_name=motrix-test-natpmp
