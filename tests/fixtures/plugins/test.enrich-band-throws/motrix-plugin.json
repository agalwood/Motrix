{
  "manifestVersion": 1,
  "id": "test.enrich-band-throws",
  "name": "Enrich Throws",
  "version": "1.0.0",
  "description": "Enrich-role plugin that throws (fail-open isolation)",
  "categories": ["integration"],
  "engines": { "motrix": ">=2.0.0" },
  "main": "dist/plugin.js",
  "permissions": [],
  "hostPermissions": ["*://*/*"],
  "activationEvents": ["onTaskType:http"],
  "contributes": {
    "hooks": { "beforeCreate": { "role": "enrich" } }
  }
}
