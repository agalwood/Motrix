{
  "name": "@motrix/native-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "cargo test --locked && node --test build.test.mjs",
    "fmt": "cargo fmt --check",
    "clippy": "cargo clippy --locked --all-targets -- -D warnings"
  }
}
