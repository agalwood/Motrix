# Bind both published services to the Docker host loopback for a host proxy.
MOTRIX_WEB_BIND_IP=127.0.0.1
MOTRIX_MDXP_BIND_IP=127.0.0.1

# Set this separately to the externally reachable HTTPS Web URL, for example:
# MOTRIX_PUBLIC_URL=https://motrix.example.com
